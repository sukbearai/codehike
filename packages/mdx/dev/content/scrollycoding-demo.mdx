<CH.Scrollycoding  showCopyButton={false} rows="focus" showExpandButton={false}>

```ts nuxt.config.ts
export default defineNuxtConfig({
  extends: ['@nuxt/ui-pro'],
  imports: {
    autoImport: true,
  },
  modules: ['@nuxt/ui'],
  devtools: { enabled: true },
})
```

---

```ts nuxt.config.ts focus=2:4
export default defineNuxtConfig({
  build: {
    transpile: ['vue-echarts'],
  },
  extends: ['@nuxt/ui-pro'],
  imports: {
    autoImport: true,
  },
  modules: ['@nuxt/ui'],
  devtools: { enabled: true },
})
```

</CH.Scrollycoding>
