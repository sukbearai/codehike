<CH.Scrollycoding  showCopyButton={false} rows="focus" showExpandButton={false}>

```vue pages/use-tsx/index.vue
<script setup lang="ts">
const msg = ref('Nuxt3!')
</script>

<template>
  <div class="page">
    Hello, {{msg}}
  </div>
</template>
```

---

```vue pages/use-tsx/index.vue focus=4:6
<script setup lang="tsx">
const msg = ref('Nuxt3!')

const Foo = (props: { msg: string }) => {
  return <div>{props.msg}</div>
}
</script>

<template>
  <div class="page">
    Hello, {{msg}}
  </div>
</template>
```

---

```vue pages/use-tsx/index.vue focus=13:13
<script setup lang="tsx">
const msg = ref('Nuxt3!')

const Foo = (props: { msg: string }) => {
  return <div>{props.msg}</div>
}
</script>

<template>
  <div class="page">
    Hello, {{msg}}

    <Foo msg="这是一个TSX组件" />
  </div>
</template>
```

</CH.Scrollycoding>
