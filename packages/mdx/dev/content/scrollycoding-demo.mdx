<CH.Scrollycoding  showCopyButton={false} rows="focus" showExpandButton={false}>

```vue pages/profile.vue
<script setup>
import User from './User.vue'
</script>
```

---

```vue pages/profile.vue focus=4:9
<script setup>
import User from './User.vue'

const user = {
  name: 'suk.bear',
  bio: 'lucky every day.',
  email: 'suk.bear.suwu@gmail.com',
  address: 'China'
}
</script>
```

---

```vue pages/profile.vue focus=13:18
<script setup>
import User from './User.vue'

const user = {
  name: 'suk.bear',
  bio: 'lucky every day.',
  email: 'suk.bear.suwu@gmail.com',
  address: 'China'
}
</script>

<template>
  <User 
    :name="user.name"
    :bio="user.bio"
    :email="user.email"
    :address="user.address"
  />
</template>
```

---

```vue pages/profile.vue focus=13:13
<script setup>
import User from './User.vue'

const user = {
  name: 'suk.bear',
  bio: 'lucky every day.',
  email: 'suk.bear.suwu@gmail.com',
  address: 'China'
}
</script>

<template>
  <User v-bind="user"/>
</template>
```

---

```vue pages/profile.vue focus=4:14
<script setup>
import User from './User.vue'

const userEventHandlers = {
  updateName(newName) {
    // ...
  },
  deleteUser(uid) {
    // ...
  },
  addFriend(friend) {
    // ...
  }
}

const user = {
  name: 'suk.bear',
  bio: 'lucky every day.',
  email: 'suk.bear.suwu@gmail.com',
  address: 'China'
}
</script>

<template>
  <User v-bind="user"/>
</template>
```

---

```vue pages/profile.vue focus=25:25
<script setup>
import User from './User.vue'

const userEventHandlers = {
  updateName(newName) {
    // ...
  },
  deleteUser(uid) {
    // ...
  },
  addFriend(friend) {
    // ...
  }
}

const user = {
  name: 'suk.bear',
  bio: 'lucky every day.',
  email: 'suk.bear.suwu@gmail.com',
  address: 'China'
}
</script>

<template>
  <User v-bind="user" v-on="userEventHandlers"/>
</template>
```

</CH.Scrollycoding>
